# Postgres connection
DATABASE_URL=postgresql://tracker:tracker@localhost:5432/tracker

# Session
SESSION_SECRET=change-me-to-a-random-string

# Password hash (Argon2id) for the single-user login
PASSWORD_HASH=

# CORS
CORS_ORIGIN=http://localhost:3000
