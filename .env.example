# Owner role (DDL, migrations only). Used by scripts/migrate.sh.
MIGRATION_DATABASE_URL=postgresql://tracker:tracker@localhost:5432/tracker

# App role (DML, RLS enforced). Used by the web process.
DATABASE_URL=postgresql://app:app@localhost:5432/tracker

# Auth mode: "none" (default, localhost only) or "proxy" (trust reverse proxy header)
AUTH_MODE=none

# Header name the reverse proxy sets (required when AUTH_MODE=proxy)
# AUTH_PROXY_HEADER=x-amzn-oidc-data

# Cognito logout configuration (required when AUTH_MODE=proxy)
# COGNITO_DOMAIN=auth.example.com
# COGNITO_CLIENT_ID=abc123def456

# Bind address (default 127.0.0.1; set to 0.0.0.0 for Docker/proxy)
HOST=127.0.0.1

# CORS
CORS_ORIGIN=http://localhost:3000

# --- Site service (public site on port 3001, serves the root domain) ---
# By default, builds from apps/site/ — a minimal redirect stub to the app.
#
# To use your own site (any repo with a Dockerfile that listens on PORT):
#   Local/Hetzner:  set SITE_PATH below and run `make up`
#   AWS (CDK):      set "siteRepo" in cdk.context.local.json — CDK clones it
#                   to EC2 and sets SITE_PATH automatically on each deploy.

# URL of the app (site uses this for "Sign in" / "Go to app" links)
# APP_DOMAIN=https://app.myfinance.com

# Path to a custom site directory with a Dockerfile (overrides apps/site/)
# Example: SITE_PATH=../my-custom-landing
# SITE_PATH=
