{
  "name": "expense-budget-tracker-sql-api",
  "version": "1.2.2",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "npm --prefix ../.. run build --workspace @expense-budget-tracker/agent-shared",
    "prelint": "npm --prefix ../.. run build --workspace @expense-budget-tracker/agent-shared",
    "build": "rm -rf dist && tsc",
    "lint": "tsc --noEmit",
    "pretest": "npm run build",
    "test": "node --test \"dist/**/*.test.js\""
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.1108.0",
    "@expense-budget-tracker/agent-shared": "1.2.2",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "hono": "^4.13.2",
    "pg": "^8.23.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.162",
    "@types/node": "^24.13.3",
    "@types/pg": "^8.21.0",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": "24.x"
  }
}
